<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cocktail Recipe</title>
  <link rel="stylesheet" href="/public/styles.main.css">
</head>
<body>
  <div class="container">
    <h1>Random Cocktail Recipe</h1>
    <!-- Add this section inside the .container div -->
    <div class="carousel">
      <div class="carousel-images">
        <img src="/images/cocktail1.png" alt="Cocktail 1">
        <img src="/images/cocktail2.png" alt="Cocktail 2">
        <img src="/images/cocktail3.png" alt="Cocktail 3">
      </div>
      <button class="prev">&#10094;</button>
      <button class="next">&#10095;</button>
    </div>

    <div class="cocktail">
      <img src="<%= cocktail.strDrinkThumb %>" alt="<%= cocktail.strDrink %>">
      <h2><%= cocktail.strDrink %></h2>
      <p><strong>Category:</strong> <%= cocktail.strCategory %></p>
      <p><strong>Instructions:</strong> <%= cocktail.strInstructions %></p>
      <h3>Ingredients:</h3>
      <ul>
        <% for (let i = 1; i <= 15; i++) { %>
          <% if (cocktail[`strIngredient${i}`]) { %>
            <li><%= cocktail[`strIngredient${i}`] %> - <%= cocktail[`strMeasure${i}`] %></li>
          <% } %>
        <% } %>
      </ul>
    </div>
  </div>
</body>
</html>
