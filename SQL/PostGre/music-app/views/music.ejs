<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Music Page</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Welcome to the Music Page</h1>

  <% if (music.length === 0) { %>
    <p>No music found. Start by adding some albums!</p>
  <% } else { %>
    <ul class="music-list">
      <% music.forEach(item => { %>
        <li class="music-item">
          <img src="<%= item.album_cover_url %>" alt="<%= item.title %> cover" width="150" height="150">
          <div class="music-info">
            <h2><%= item.title %></h2>
            <p><strong>Artist:</strong> <%= item.artist %></p>
            <p><strong>Album:</strong> <%= item.album %></p>
          </div>
        </li>
      <% }); %>
    </ul>
  <% } %>
</body>
</html>
